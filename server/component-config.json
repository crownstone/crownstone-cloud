{
  "loopback-component-explorer": {
    "mountPath": "/explorer"
  },
  "loopback-component-access-groups": {
    "userModel": "user",
    "roleModel": "Role",
    "groupModel": "Group",
    "foreignKey": "groupId",
    "groupRoles": [
      "$group:admin",
      "$group:owner",
      "$group:member",
      "$group:guest"
    ]
  }
}
